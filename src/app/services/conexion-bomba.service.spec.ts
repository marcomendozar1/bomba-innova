import { ConexionBombaService } from './conexion-bomba.service';

describe('ConexionBomba.Service', () => {
  it('should create an instance', () => {
    expect(new ConexionBombaService()).toBeTruthy();
  });
});
