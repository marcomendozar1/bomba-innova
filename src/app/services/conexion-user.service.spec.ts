import { ConexionUserService } from './conexion-user.service';

describe('ConexionUser.Service', () => {
  it('should create an instance', () => {
    expect(new ConexionUserService()).toBeTruthy();
  });
});
